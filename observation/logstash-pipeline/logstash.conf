input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

# filter {
#   # No filter needed for this simple test
# }

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "test-logs-%{+YYYY.MM.dd}"
  }
}