include:
  - ../database/docker/db-compose.yaml
  - server-compose.yaml

services:
  server:
    depends_on:
      - database
    networks:
      - server-network

  database:
    networks:
      - server-network

networks:
  server-network:
    driver: bridge
